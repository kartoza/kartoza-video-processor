# Use Nix flake for development environment
use flake

# Project paths
export PROJECT_ROOT="$PWD"

# Go paths (isolated per project)
export GOPATH="$PWD/.go"
export GOCACHE="$PWD/.go/cache"
export GOMODCACHE="$PWD/.go/pkg/mod"

# Add project bin to PATH
PATH_add bin
PATH_add "$GOPATH/bin"
